# Copia este archivo como .env y ajusta la ruta si hace falta.
# Usa ruta ABSOLUTA al db/custom.db del proyecto para evitar problemas
# de resolución relativa entre Prisma CLI y el runtime de Next.
DATABASE_URL="file:/ruta/al/proyecto/db/custom.db"
